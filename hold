rake db:reset - to reset seed data

        <form id="exercise-attributes-form">
            <h2>Add a New Workout: </h2>
            <label>Title:</label>
            <input id='input-title' type="text" name="title" value="" placeholder="Title your workout" class="input-text">
            <br><br>
            <label>Date:</label>
            <input id='input-date' type="text" name="date" value="" placeholder="EX: 08/12/2021.." class="input-text">
            <br><br>
        
        </form>


                let id = attributes.id
                let date = attributes.date
                let category = attributes.category
                let calories = attributes.calories
                let duration = attributes.duration
                debugger
                const postAttributes = `<div data-id=${id}>
                
                </div>`

        let newDiv = document.createElement("div")
        let newTitle = document.createElement("h2")
            newDiv.setAttribute("data-id", `${w.id}`)
            newTitle.innerHTML = `Title: ${workout.title}`
            workoutContainer.appendChild(newDiv, newTitle)


    <form id="new-workout-form">
        <h2>Add a new workout: </h2>
        <label>Title:</label>
        <input id='input-title' type="text" name="title" value="" placeholder="Title your workout" class="input-text">
        <br><br>
        <input id='create-button' type="submit" name="submit" value="Add Workout" class="submit">
    </form>